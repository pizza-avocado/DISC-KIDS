<div class = "container">
	<div class="back-rgba">

          			<div class = "col-lg-4"><%= attachment_image_tag @item, :jacket_image, class: "image", fallback: "no_images.png", size: '200x200' %>
				    <% if @item.status == "在庫あり" or @item.stock.to_i == 0 %>
					    <div class = "link"><%= button_to "カートに追加する", carts_path, params: { item_id: @item.id }, class: "btn btn-primary btn-lg" %>
					    </div>
				    <% else %>
					    <div class = "link"><button type="button" class="btn btn-default btn-lg" disabled="disabled">カートに追加する</button></div>
				    <% end %>
				    <% if user_signed_in? %>
						<% if @item.liked_by?(current_user) %>
						<li>
							<%= link_to item_likes_path(@item), method: :delete do %>
							<i class = "fa fa-heart" aria-hidden = "true" style = "color:red;"></i>
							<%= @item.likes.count %> いいね!
							<% end %>
						</li>
						<% else %>
						<li>
							<%= link_to item_likes_path(@item), method: :post do %>
							<i class = "fa fa-heart-o" aria-hidden = "true"></i>
							<%= @item.likes.count %> !
						</li>
						<% end %>
						<% end %>
					<% end %>
			    </div>



					<p>商品名: <%= @item.name %> </p>
					<p>アーティスト名: <%= @item.artist.artist %></p>
					<p>ジャンル名: <%= @item.genre.genre %></p>
					<p>レーベル名: <%= @item.label.label %></p>
					<p>価格: <%= @item.price %></p>
					<p>在庫数: <%= @item.stock %></p>
					<p>販売ステータス: <%= @item.status %></p>

				</div>
				<div class= "row">
					<div class= "width">
						<table class="table col-lg-4">

							<thead>
								<tr>
									<th>楽曲リスト</th>
								</tr>
							</thead>

							<tbody>
								<% @item.discs.each do |disc| %>
								<tr>
									<td>ディスク<%= disc.disc_number %> </td>
								</tr>
									<% disc.tracks.each do |track| %>

									<td>トラック<%= track.track_number %> </td>
									<td><%= track.track_name %> </td>
									<td></td>
									<td></td>
									<td></td>
								</tr>
									<% end %>
								</tr>
								<% end %>
							</tbody>
						</table>
					</div>
				</div>
			</div>
	</div>
</div>


